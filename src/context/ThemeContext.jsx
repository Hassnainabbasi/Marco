import { createTheme, ThemeProvider } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    teal: {
      700: '#00796b', // Add the correct teal shade you need
      800: '#004d40',
    },
  },
});
